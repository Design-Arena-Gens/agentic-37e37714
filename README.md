# "بوصلة الحياة" Storyboard

واجهة تفاعلية بسيطة تعرض مخطط الستوري بورد لفيديو دعائي مدته 12–15 ثانية لكتاب "بوصلة الحياة".

## 🚀 البدء

```bash
npm install
npm run dev
# افتح http://localhost:3000
```

## 📦 سكربتات npm

- `npm run dev` تشغيل وضع التطوير
- `npm run build` بناء نسخة الإنتاج
- `npm run start` تشغيل خادم الإنتاج المحلي
- `npm run lint` فحص الشفرة عبر ESLint

## 🧩 المكوّنات الرئيسية

- `app/page.jsx` يعرض المشاهد الخمس مع الزمن، الاقتراح البصري، الصوتيات، وملاحظات الإخراج.
- `app/globals.css` تنسيق داكن مع لمسات مضيئة تعكس طابع الفيديو الملهم.
- `app/layout.jsx` يضبط اللغة إلى العربية ودعم الاتجاه من اليمين لليسار.

## 📝 ملاحظات تنفيذية

- تم استخدام Next.js (App Router) لتوليد صفحة ثابتة قابلة للنشر على Vercel.
- الخط العربي "Tajawal" مضمّن عبر Google Fonts لإحساس بصري متناسق.
- التصميم يستجيب للأحجام المختلفة مع انتقالات خفيفة عند التفاعل.

يمكنك تخصيص المحتوى أو إضافة عناصر مرئية إضافية بما يتناسب مع إنتاج الفيديو.
